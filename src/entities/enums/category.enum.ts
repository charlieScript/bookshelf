export enum CATEGORY {
  comic = 'comic',
  fantasy = 'fantasy',
  action = 'action',
  thriller = 'thriller',
  comtemporary = 'comtemporary',
}

export const categoryArray: CATEGORY[] = [
 CATEGORY.action, CATEGORY.comic, CATEGORY.fantasy, CATEGORY.thriller, CATEGORY.comtemporary
]

export enum ROLES {
  OWNER = 'OWNER',
  READER = 'READER'
}